[general]
default_prefix = ","
default_nickname = "OneBot"

plugin_path = "plugins"
plugins = ["parrot"]

protocol_path = "protocols"
protocols = ["matrix"]

[database]
engine = "leveldb" # valid values are 'leveldb' or 'mongodb'

leveldb_path = "onedb"
